# As dependências são declaradas em pyproject.toml — fonte única, pra não
# divergirem. Este arquivo existe porque o README documenta
# `pip install -r requirements.txt`, e mantê-lo funcionando é mais barato que
# reeducar quem clona o repo.
-e .[dev]
